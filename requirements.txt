# Core pipeline
# picamera2>=0.3.20        # Installed via apt (python3-picamera2) on Raspberry Pi OS Bookworm
opencv-python-headless==4.9.0.80
numpy>=1.26.0,<2.0.0
flask>=3.0.0
requests>=2.31.0

# OCR
# rapidocr_onnxruntime is the PRIMARY engine (config.OCR_ENGINE = "paddleocr"),
# not an optional extra. It does text *detection* then recognition, which is
# what real bus photos need: the route board is often ~2% of the frame. Without
# it the pipeline falls back to Tesseract --psm 7, which assumes the whole image
# is one line of text and scores 1/8 on the benchmark set versus 7/8 with this.
rapidocr_onnxruntime>=1.4.0
pytesseract>=0.3.10            # fallback engine only
# tesseract-ocr binary itself is installed via apt/package manager — see guide

# GPIO (Only needed on physical Raspberry Pi hardware)
# gpiozero>=2.0.1

# Optional: only needed if config.OCR_ENGINE = "easyocr"
# easyocr>=1.7.1

# Optional: only needed if config.TTS_ENGINE = "piper"
# piper-tts>=1.6.0

# Optional: only needed if config.POWER_MONITORING_ENABLED = True
# adafruit-circuitpython-ads1x15>=2.2.0
