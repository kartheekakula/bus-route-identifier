# Core pipeline
# picamera2>=0.3.20        # Installed via apt (python3-picamera2) on Raspberry Pi OS Bookworm
opencv-python-headless==4.9.0.80
numpy>=1.26.0,<2.0.0
flask>=3.0.0
requests>=2.31.0

# OCR
pytesseract>=0.3.10
# tesseract-ocr binary itself is installed via apt/package manager — see guide
#
# THE REAL OCR ENGINE LIVES IN requirements-ocr.txt — install that too unless
# you are deploying to Vercel:
#
#     pip install -r requirements.txt -r requirements-ocr.txt
#
# This file is deliberately kept serverless-safe. rapidocr_onnxruntime depends
# on opencv-python (not -headless), which needs libGL.so.1; that library does
# not exist on Vercel's runtime, so pulling it in here makes `import cv2` fail
# at startup with an ImportError before the app can serve anything.

# GPIO (Only needed on physical Raspberry Pi hardware)
# gpiozero>=2.0.1

# Optional: only needed if config.OCR_ENGINE = "easyocr"
# easyocr>=1.7.1

# Optional: only needed if config.TTS_ENGINE = "piper"
# piper-tts>=1.6.0

# Optional: only needed if config.POWER_MONITORING_ENABLED = True
# adafruit-circuitpython-ads1x15>=2.2.0
